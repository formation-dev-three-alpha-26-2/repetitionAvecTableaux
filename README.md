# repetitionAvecTableaux


## Exigences de base

---

## 1 — Parcourir un tableau avec for
Écrivez une fonction appelée **tableauFor** qui prend un tableau en paramètre, parcourt tous les éléments avec une boucle `for` et les affiche dans la console.

```javascript
function tableauFor(tableau) {
}
//tableauFor([1, 2, 3]) ->  1, 2, 3
```

---

## 2 — Parcourir un tableau avec while
Écrivez une fonction appelée **tableauWhile** qui prend un tableau en paramètre, parcourt tous les éléments avec une boucle `while` et les affiche dans la console.

```javascript
function tableauWhile(tableau) {
}
// tableauWhile([1, 2, 3]) -> affiche 1, 2, 3
```

---

## 3 — Somme d’un tableau
Écrivez une fonction appelée **somme** qui prend un tableau de nombres et retourne la somme des nombres.

```javascript
function somme(tableau) {
}
// somme([1, 2, 3]) -> 6
```

---

## 4 — Somme un élément sur deux
Écrivez une fonction appelée **sommeUnSurDeux** qui retourne la somme d’un nombre sur deux en commençant par le premier.

```javascript
function sommeUnSurDeux(tableau) {
}
// sommeUnSurDeux([1, 2, 3, 4, 5]) -> 9
```

---

## 5 — Somme à partir d’un index
Écrivez une fonction appelée **sommeDepuisIndex** qui retourne la somme à partir d’un index jusqu’à la fin du tableau.

```javascript
function sommeDepuisIndex(tableau, index) {
}
//sommeDepuisIndex([1,2,3,4,5],1) -> 14
```

---

## 6 — Somme jusqu’à un index
Écrivez une fonction appelée **sommeJusquaIndex** qui retourne la somme de l’index 0 jusqu’à l’index donné.

```javascript
function sommeJusquaIndex(tableau, index) {
}
// sommeJusquaIndex([1,2,3,4,5],2) -> 6
```

---

## 7 — Soustraction inversée
Écrivez une fonction appelée **soustractionInverse** qui soustrait les éléments du dernier au premier.

```javascript
function soustractionInverse(tableau) {
}
// soustractionInverse([1,2,3]) -> 0
```

---

## 8 — Produit d’un tableau
Écrivez une fonction appelée **produit** qui retourne le produit de tous les éléments du tableau.

```javascript
function produit(tableau) {
}
// produit([1,2,3]) -> 6
```

---

## 9 — Moyenne d’un tableau
Écrivez une fonction appelée **moyenne** qui retourne la moyenne des éléments.

```javascript
function moyenne(tableau) {
}
// moyenne([1,2,3]) -> 2
```

---

## 10 — Carré des éléments
Écrivez une fonction appelée **carre** qui retourne un nouveau tableau avec chaque élément au carré.

```javascript
function carre(tableau) {
}
//carre([1,2,3]) -> [1,4,9]
```

---

## 11 — Vérifier si c’est un tableau
Écrivez une fonction appelée **estTableau** qui retourne `true` si c’est un tableau, sinon `false`.

```javascript
function estTableau(valeur) {
}
//estTableau([1,2,3]) -> true
```

---

# Plus de pratique

---

## 12 — Minimum  
Écrivez une fonction appelée **minimum** qui retourne le plus petit nombre du tableau.

```javascript
function minimum(tableau) {
}
```

Exemple :
```javascript
minimum([1, 2, 3]); // -> 1
```

---

## 13 — Maximum  
Écrivez une fonction appelée **maximum** qui retourne le plus grand nombre du tableau.

```javascript
function maximum(tableau) {
}
```

Exemple :
```javascript
maximum([1, 2, 3]); // -> 3
```

---

## 14 — Longueur de la plus courte chaîne  
Écrivez une fonction appelée **chaineLaPlusCourte** qui retourne la longueur de la chaîne la plus courte.

```javascript
function chaineLaPlusCourte(tableau) {
}
```

Exemple :
```javascript
chaineLaPlusCourte(["hi", "cat", "hello"]); // -> 2
```

---

## 15 — Longueur de la plus longue chaîne  
Écrivez une fonction appelée **chaineLaPlusLongue** qui retourne la longueur de la chaîne la plus longue.

```javascript
function chaineLaPlusLongue(tableau) {
}
```

Exemple :
```javascript
chaineLaPlusLongue(["hi", "cat", "hello"]); // -> 5
```

---

## 16 — Plus courte et plus longue chaîne  
Écrivez une fonction appelée **plusCourtePlusLongue** qui retourne un tableau contenant la plus courte et la plus longue chaîne.

```javascript
function plusCourtePlusLongue(tableau) {
}
```

Exemple :
```javascript
plusCourtePlusLongue(["hi", "cat", "hello"]); // -> ["hi", "hello"]
```

---

## 17 — Minimum et maximum  
Écrivez une fonction appelée **minimumMaximum** qui retourne un tableau contenant le plus petit et le plus grand nombre.

```javascript
function minimumMaximum(tableau) {
}
```

Exemple :
```javascript
minimumMaximum([1, 5, 4, 3]); // -> [1, 5]
```

---

## 18 — Multiplier par un nombre  
Écrivez une fonction appelée **multiplierPar** qui retourne un nouveau tableau avec chaque élément multiplié par un nombre donné.

```javascript
function multiplierPar(tableau, nombre) {
}
```

Exemple :
```javascript
multiplierPar([1, 5, 4, 3], 2); // -> [2, 10, 8, 6]
```

---

## 19 — Multiplier par l’index  
Écrivez une fonction appelée **multiplierParIndex** qui multiplie chaque élément par son index.

```javascript
function multiplierParIndex(tableau) {
}
```

Exemple :
```javascript
multiplierParIndex([1, 5, 4, 3]); // -> [0, 5, 8, 9]
```

---

## 20 — Longueur des chaînes  
Écrivez une fonction appelée **longueurs** qui retourne un tableau contenant la longueur de chaque chaîne.

```javascript
function longueurs(tableau) {
}
```

Exemple :
```javascript
longueurs(["hi", "cat", "hello"]); // -> [2, 3, 5]
```

---

## 21 — Total des caractères  
Écrivez une fonction appelée **totalCaracteres** qui retourne le nombre total de caractères.

```javascript
function totalCaracteres(tableau) {
}
```

Exemple :
```javascript
totalCaracteres(["hi", "cat", "hello"]); // -> 10
```

---

# Avancé

---

## 22 — Mots de longueur paire  
Écrivez une fonction appelée **filtrerMotsPairs** qui retourne uniquement les mots dont la longueur est paire.

```javascript
function filtrerMotsPairs(tableau) {
}
```

Exemple :
```javascript
filtrerMotsPairs(["hi", "cat", "hello"]); // -> ["hi"]
```

---

## 23 — Supprimer le dernier élément de chaque tableau  
Écrivez une fonction appelée **supprimerDernierDeChaque** qui supprime le dernier élément de chaque tableau.

```javascript
function supprimerDernierDeChaque(tableaux) {
}
```

Exemple :
```javascript
supprimerDernierDeChaque([[1,2,3],[4,5]]); // -> [[1,2],[4]]
```

---

## 24 — Ajouter un élément à chaque tableau  
Écrivez une fonction appelée **ajouterDernierAChaque** qui ajoute un élément à la fin de chaque tableau.

```javascript
function ajouterDernierAChaque(tableaux, element) {
}
```

Exemple :
```javascript
ajouterDernierAChaque([[2,3],[2]], 1); // -> [[2,3,1],[2,1]]
```

---

## 25 — Somme de tableaux imbriqués  
Écrivez une fonction appelée **sommeTableaux** qui retourne la somme de tous les éléments des tableaux imbriqués.

```javascript
function sommeTableaux(tableaux) {
}
```

Exemple :
```javascript
sommeTableaux([[1,2],[3,4]]); // -> 10
```

---

## 26 — Multiplier par le plus petit  
Écrivez une fonction appelée **multiplierParPlusPetit** qui multiplie chaque élément par le plus petit nombre du tableau.

```javascript
function multiplierParPlusPetit(tableau) {
}
```

Exemple :
```javascript
multiplierParPlusPetit([2,3,4]); // -> [4,6,8]
```

---

## 27 — Fusionner les tableaux  
Écrivez une fonction appelée **fusionnerTableaux** qui retourne un seul tableau contenant tous les éléments.

```javascript
function fusionnerTableaux(tableaux) {
}
```

Exemple :
```javascript
fusionnerTableaux([[2,3],[2],[3,4]]); // -> [2,3,2,3,4]
```

---

## 28 — Somme pairs et impairs  
Écrivez une fonction appelée **sommePairsImpairs** qui retourne un tableau avec la somme des impairs et la somme des pairs.

```javascript
function sommePairsImpairs(tableau) {
}
```

Exemple :
```javascript
sommePairsImpairs([1,3,2,4]); // -> [4,6]
```

---

## 29 — Plus courte chaîne dans un tableau mixte  
Écrivez une fonction appelée **plusCourteChaineMixte** qui retourne la plus courte chaîne dans un tableau mixte.

```javascript
function plusCourteChaineMixte(tableau) {
}
```

Exemple :
```javascript
plusCourteChaineMixte([4, "two", 2, "three"]); // -> "two"
